## 1. Constants and Libs

- [x] 1.1 Update `OutputType` and constants in `src/lib/test-plan.ts`
- [x] 1.2 Update path inference logic in `src/lib/test-plan.ts` to use `.finalrun/suites/`

## 2. Commands

- [x] 2.1 Update `plan` command in `src/commands/plan.ts` (strings and default paths)
- [x] 2.2 Update `apply` command in `src/commands/apply.ts` (instruction templates)
- [x] 2.3 Update `validate` command in `src/commands/validate.ts` (path recognition and error messages)

## 3. Documentation

- [x] 3.1 Update `README.md` with new path and migration notice
- [x] 3.2 Update any other documentation or examples in the repo

## 4. Verification

- [x] 4.1 Run existing tests to ensure no regressions
- [x] 4.2 Manually verify `plan`, `apply`, and `validate` with a sample feature
