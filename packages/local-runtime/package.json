{
  "name": "@finalrun/local-runtime",
  "version": "0.1.7",
  "private": true,
  "description": "Per-platform runtime bundle for local FinalRun test execution. Builds finalrun-runtime-<version>-<platform>.tar.gz with on-disk assets (driver APKs/iOS zips, gRPC proto, Vite SPA dist) that the Bun-compiled CLI binary needs at runtime. Not published to npm — uploaded to GitHub Releases.",
  "license": "Apache-2.0",
  "scripts": {
    "build:tarball": "node scripts/buildRuntimeTarball.mjs"
  }
}
