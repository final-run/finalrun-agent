// Root layout — mirrors the <head> the legacy string renderers emitted:
// the DM Sans font + a UTF-8 charset. The body is intentionally bare so
// individual pages can control their own <main> shells.

import type { ReactNode } from 'react';

export const metadata = {
  title: 'FinalRun Reports',
};

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <head>
        <meta charSet="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
        <link
          href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&display=swap"
          rel="stylesheet"
        />
      </head>
      <body>{children}</body>
    </html>
  );
}
