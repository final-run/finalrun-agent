# Tasks: Multi-Device Test Orchestration (v1: 2 Devices, Same Platform)

**Change**: 260415-1mzp-multi-device-orchestration
**Spec**: `spec.md`
**Intake**: `intake.md`

<!--
  TASK FORMAT: - [ ] {ID} [{markers}] {Description with file paths}

  Markers:
    [P]   — Parallelizable (different files, no dependencies on other [P] tasks in same group)

  Phase 1 = Setup (types, loader, workspace, recording-manager fix)
  Phase 2 = Core Implementation (planner prompt, planMulti, session runner, orchestrator)
  Phase 3 = Integration (CLI routing, report writer, report-web sandwich UI)
  Phase 4 = Polish (fixtures, regression artifacts, quickstart doc)
-->

## Phase 1: Setup

<!-- Foundation: types, loader/compiler, workspace relaxation, non-breaking RecordingManager fix. All Phase 1 work is additive/gated; single-device paths remain byte-identical at the end of this phase. -->

- [x] T001 [P] Create `packages/common/src/models/MultiDeviceConfig.ts` defining `DeviceDefinition { platform: string; app: string }` and `MultiDeviceConfig { devices: Record<string, DeviceDefinition> }`. Re-export from `packages/common/src/index.ts`.
- [x] T002 [P] Extend `packages/common/src/models/TestResult.ts` — add optional `device?: string` to `AgentAction` (line ~23) and optional `perDeviceArtifacts?: Record<string, { folder: string; recordingFile?: string; deviceLogFile?: string; recordingStartedAt?: string }>` plus optional `multiDevice?: boolean` discriminator to `TestResult` (line ~45). Ensure JSON emission omits absent fields.
- [x] T003 [P] Extend `packages/common/src/models/RunManifest.ts` — add optional `multiDevice?: { devices: Record<string, { platform: string; app?: string; hardwareName: string }> }` (line ~64). Ensure single-device runs emit JSON byte-identical to baseline.
- [x] T004 [P] Modify `packages/device-node/src/device/RecordingManager.ts` — change `getMapKey(runId, testId)` (line 72) to `getMapKey(runId, testId, deviceId?: string)` returning `${runId}###${testId}` when `deviceId` is undefined/empty (byte-identical) or `${runId}###${testId}###${deviceId}` when provided. Keep all internal 2-arg call sites unchanged (do not pass `deviceId`). Sanitize `deviceId` with the existing `_sanitizeForFilename` helper before concatenating.
- [x] T005 Create `packages/cli/src/multiDeviceTestLoader.ts` — parse `.finalrun/multi-device/devices.yaml`, tests, and suites. Validation rules: (a) exactly 2 device entries; (b) both platforms identical; (c) v1 rejects any `platform !== 'android'` with a clear error; (d) every step string must contain at least one `${devices.X}` token where X is a defined key; (e) token regex `/\$\{(variables|secrets|devices)\.([A-Za-z0-9_-]+)\}/g`; (f) unknown device key → fail with device list; (g) suites may reference multi-device tests only. Expose `collectAllMultiDeviceTests(workspace)` helper that parallels `collectAllTests` for the multi-device subtree.
- [x] T006 Create `packages/cli/src/multiDeviceTestCompiler.ts` — interpolate `${variables.*}` using the same environment resolver as `testCompiler.ts`. Preserve `${devices.*}` and `${secrets.*}` tokens literally. Emit a compiled goal string with a `Devices:` header block listing each device's `{key, platform, app}` plus the numbered step list. Unit-testable pure function; no side effects.
- [x] T007 Modify `packages/cli/src/workspace.ts` — add optional `multiDeviceDir`, `multiDeviceTestsDir`, `multiDeviceSuitesDir` to `FinalRunWorkspace` (line 15). Populate them in `buildWorkspace()` (line 501). Relax `ensureWorkspaceDirectories()` (line 148) to accept at least one of `tests/` OR `multi-device/tests/`; error message when neither exists: `Missing .finalrun/tests or .finalrun/multi-device/tests directory`. Update `resolveSuiteManifestPath()` (line 307) to handle a `multi-device/suites/` prefix by resolving against `finalrunDir`.
- [x] T008 Modify `packages/cli/src/testSelection.ts` — at the top of `resolveSelectorPath()` (line 118) add a prefix check: if the selector starts with `multi-device/tests/`, resolve against `finalrunDir` and return. Otherwise fall through untouched. In `collectAllTests()` (line 27), when `testsDir` is absent, return `[]` (do not throw) so the caller can surface an improved error naming both `tests/` and `multi-device/tests/`.
- [x] T009 Modify `packages/cli/src/testRunner.ts` (line ~101) and `packages/cli/bin/finalrun.ts` (line ~143) — early-exit branch: if the resolved selector path begins with `multi-device/tests/`, route to the multi-device orchestrator (stub method from T019, injected later); otherwise fall through to the existing `TestExecutor` path. Improve the no-tests error to mention `multi-device/tests/` when applicable.

## Phase 2: Core Implementation

<!-- Planner prompt, planMulti API, session runner, orchestrator loop. All Phase 2 additions are sibling modules — no existing single-device module is modified beyond the optional fields added in Phase 1. -->

- [x] T010 [P] Create `packages/goal-executor/src/prompts/multi-device-planner.md`. Mirror `planner.md` structure: `<inputs>` lists both devices with platforms; `<turn_loop>` references per-active-device pre/post/hierarchy blocks; `<output_schema>` replaces single `action` with `actions: [{device, action}]` (length 1 or 2); `remember` becomes `[{device, note}]`; `<actions>` catalog copied verbatim; `<stagnation_and_retries>` scopes element identity as `{device}:{element}`; adds Cross-Device Causality Protocol ("alice's step produces an effect on bob — next iteration activates bob, first observation shows the effect, possibly after a `wait`") and Parallel Actions Protocol ("2 actions iff the current step references both devices AND actions are independent; duplicate-device responses will be rejected"); adds Devices Header ("You control exactly 2 devices named {{key}} on platform {{platform}}").
- [x] T011 [P] Add types in `packages/goal-executor/src/ai/AIAgent.ts` (after line ~96, sibling to existing `PlannerRequest`/`PlannerResponse`): `PlannerAction` structural type with fields `{ act, reason, text?, clearText?, direction?, durationSeconds?, url?, repeat?, delayBetweenTapMs?, result?, analysis?, severity? }`; `MultiDevicePlannerRequest { testObjective, devices, activeDeviceStates: Record<string, {preActionScreenshot?, postActionScreenshot, hierarchy, platform}>, history?, remember?, preContext?, traceStep? }`; `MultiDevicePlannerResponse { actions: Array<{device, action: PlannerAction}>, remember?: Array<{device, note}>, thought?: {plan?, think?, act?}, trace?: LLMTrace }`. Do NOT modify `PlannerResponse` or `PlannerRequest`.
- [x] T012 Add `AIAgent.planMulti(request: MultiDevicePlannerRequest): Promise<MultiDevicePlannerResponse>` in `packages/goal-executor/src/ai/AIAgent.ts`. Reuse the same Vercel AI SDK `generateText` + `Output.json()` path as `plan()` (line ~480). Build the user message programmatically: one per-active-device block (pre/post/hierarchy/platform) followed by history and remember. Use the same `maxOutputTokens` budget as `plan()`. Load the new prompt from `multi-device-planner.md` via the existing prompt loader. Validate response: reject if `actions.length > 2`, if any action references an unknown device key, or if the same device appears twice. On rejection, retry once with an error hint; then throw.
- [x] T012a **DEFERRED (code-review only; no live emulators in this worktree)** gRPC-concurrency spike (Risk B / assumption #23). Substitute deliverable — static code-review of shared-mutable-state hot spots in the `DeviceNode.getInstance()` singleton path below `setUpDevice()`:
  - `DeviceNode.setUpDevice(deviceInfo)` (DeviceNode.ts:118) — the only singleton-scoped mutable state is `_devicePool` (keyed by `deviceInfo.id`, safe for two distinct devices) and `_initCleanupPromise` (awaited once per call; idempotent). Parallel calls for two distinct device IDs do not share per-call state.
  - `GrpcDriverSetup.setUp()` (GrpcDriverSetup.ts:99) — constructs a **fresh** `GrpcDriverClient` per call via `_grpcClientFactory()`. No per-instance channel is shared across calls; each setup owns its channel end-to-end. Helpers (`_androidDeviceSetup`, `_iosSimulatorSetup`) are **stateless** after construction — they capture constructor deps but accumulate no per-call state on the instance.
  - `AdbClient.forwardPort()` (AdbClient.ts:173) — port allocation is already serialized behind `_withAllocationLock` (async mutex at AdbClient.ts:150). Concurrent allocations for two devices return distinct ports deterministically.
  - `GrpcDriverClient` — each instance owns its own channel/stub; `Promise.all([a.executeAction, b.executeAction])` operates on disjoint instances with disjoint channels.
  - No global mutable module-level state found in `packages/device-node/src/grpc/` or `DeviceNode.ts` aside from the singleton `_instance` (idempotent) and the in-memory `_portMap`/`_allocatedPorts` (mutex-protected).
  - **Conclusion**: the existing design already supports `Promise.all([setUpDevice(deviceA), setUpDevice(deviceB)])` and parallel `executeAction` dispatch on the two resulting independent gRPC clients without a singleton refactor. No blocker for T013/T014. Risk B mitigated; spike to be re-run live when emulators are available as part of T028 (E2E smoke). <!-- clarified: 2026-04-15 — surfaced Risk B spike as an explicit gating task per plan §Risks B and spec assumption #23; was missing from tasks. -->
- [x] T013 Create `packages/cli/src/multiDeviceSessionRunner.ts` exporting `prepareMultiDeviceTestSession(deviceConfig: MultiDeviceConfig, config): Promise<MultiDeviceTestSession>`. Behavior: (a) call `DeviceNode.detectInventory()` to list connected/startable hardware; (b) auto-assign — iterate `deviceConfig.devices` keys, pick first unassigned detected device whose platform matches; hard-fail if fewer than 2 matches; (c) `Promise.all` boot any emulators and `setUpDevice()` for both, yielding 2 independent `DeviceAgent` + `GrpcDriverClient` instances; (d) return `MultiDeviceTestSession { devices: Map<key, { key, device, deviceInfo, platform, app, hardwareName }>, cleanup(): Promise<void> }`. `cleanup()` calls `stopRecording` and `tearDown` on both in `Promise.all` (best-effort; both must complete within 5s per fail-fast ceiling).
- [x] T014 Create `packages/goal-executor/src/MultiDeviceOrchestrator.ts` implementing the active-device-scoped loop. State: `lastKnownState: Map<deviceKey, {screenshot, hierarchy}>`, `history: Array<{iteration, device, act, reason, outcome}>`, `remember: Array<{device, note}>`, `stepIndex: number`. Internal `DeviceExecutor` class wraps one `DeviceAgent` + per-device `ActionExecutor` (existing class, new instance — do NOT modify). Per iteration: parse current step's `${devices.X}` tokens → active set (1 or 2); capture pre/post from active devices only in parallel; call `aiAgent.planMulti()`; terminal check (`COMPLETED`/`FAILED`); dispatch actions (single → sequential, two-distinct → `Promise.all`); capture post-action state; append device-tagged history; advance `stepIndex` by parsing `thought.plan` for the `[→ ...]` marker (fallback: hold `stepIndex`). Watchdog: abort test as FAIL if same `stepIndex` persists >5 consecutive iterations without terminal progress, emitting failure reason `watchdog: step {N} stuck for >5 iterations` (matches spec §Step pointer maintenance scenario). <!-- clarified: 2026-04-15 — pinned exact watchdog FAIL reason string per spec scenario, for parity with single-device Case-E. -->
- [x] T015 Thread `AbortController` through `MultiDeviceOrchestrator` — every `DeviceExecutor.dispatchAction()` accepts an `AbortSignal`. On any device's action failure / gRPC disconnect / planner-FAILED, the orchestrator calls `controller.abort()` to interrupt the sibling device's in-flight dispatch. Cancellation semantics: graceful gRPC call-stream cancel on `abort()` with a 2-second working budget; if the graceful path does not return within 2s, fall back to hard teardown of the gRPC channel. Combined cancellation + cleanup must complete within the 5-second fail-fast ceiling (2s graceful + 3s recordings/teardown per assumption #16). Surviving-device `stopRecording` must still run cleanly (call outside the abort-gated path). Do NOT add abort plumbing to the existing single-device `ActionExecutor` class — abort is scoped to the new multi-device path only. <!-- clarified: 2026-04-15 — pinned 2s graceful cancellation budget from spec assumption #16 / Phase 3 design review. -->
- [x] T016 Wire the multi-device recording lifecycle in `MultiDeviceOrchestrator` bootstrap: `Promise.all([alice.startRecording({..., deviceId: 'alice'}), bob.startRecording({..., deviceId: 'bob'})])` at test start; record per-device `startedAt` timestamps on the session object. Compute shared-timeline anchor as `min(alice.startedAt, bob.startedAt)`; compute per-device `videoOffsetMs = max(0, stepTimestamp - deviceRecordingStartedAt)` for each iteration. At teardown: `Promise.all([alice.stopRecording, bob.stopRecording])`.

## Phase 3: Integration & Edge Cases

<!-- Wire orchestrator into CLI; per-device artifact layout in reportWriter; sandwich UI + synced playback in report-web. -->

- [x] T017 Complete the CLI routing stub from T009 — in `packages/cli/src/testRunner.ts` and `packages/cli/bin/finalrun.ts`, import `MultiDeviceOrchestrator` and `prepareMultiDeviceTestSession`; dispatch multi-device selectors through them. Single-device branch untouched.
- [x] T018 Modify `packages/cli/src/reportWriter.ts` (line ~312 `writeTestRecord()`) — branch on `result.multiDevice` / `result.perDeviceArtifacts`: absent → existing single-device path UNTOUCHED; present → for each device key, create `tests/{testId}/<device>/{screenshots,actions}/` subfolders; write screenshot and action JSON scoped per device (action JSON includes `device` field); compute per-device `videoOffsetMs` from the device's `recordingStartedAt`. Step numbering: `stepNumber = iteration` (1-indexed, zero-padded 3 digits) shared across devices — parallel step at iteration 7 yields both `alice/actions/007.json` and `bob/actions/007.json`; sequential step at iteration 8 on alice only yields `alice/actions/008.json` (bob slot absent). **NOTE**: Single-device `reportWriter.ts::writeTestRecord()` remains UNTOUCHED (preservation invariant); the multi-device branch is implemented in `multiDeviceTestRunner.ts::writeMultiDeviceTestRecord()` which owns the multi-device test record layout end-to-end.
- [x] T019 Modify `packages/report-web/src/artifacts.ts` (line ~127) — when `manifest.multiDevice` present, load per-device log tails from the per-device `deviceLogFile` paths. Otherwise existing single-device path UNTOUCHED.
- [x] T020 Add sandwich-UI renderers in `packages/report-web/src/renderers.ts`. At the entry of `renderTestDetailSection()` (line ~2103) branch: `if (manifest.multiDevice) return renderMultiDeviceWorkspace(test, manifest)` else existing single-device path UNTOUCHED. New renderers: `renderMultiDeviceWorkspace()` (outer container with `background: var(--color-background-secondary); border-radius: var(--border-radius-lg); padding: 16px`), `renderTopBar()` (test name + pass/fail chip `#E1F5EE`/`#085041` pass, red variants fail, playback `◀ ▶ 0:00 / 0:00`), `renderSandwichGrid()` (`grid-template-columns: 200px minmax(0,1fr) 200px; gap: 12px; align-items: start`), `renderDeviceColumn(key, side)` with header (dot + name + platform, reversed for right column) and `<video data-device="{key}" data-role="recording-video">` (`aspect-ratio: 9/19; border-radius: 14px`), `renderStepTimelinePanel(steps)` (center panel; chat bubbles via `renderChatBubbleStep()`), `renderChatBubbleStep(step)` (alice left-aligned purple `#EEEDFE`/border `#7F77DD`/text `#26215C`; bob right-aligned green `#E1F5EE`/border `#1D9E75`/text `#04342C`; parallel full-width dashed bordered centered; selected bubble adds `box-shadow: 0 0 0 2px #AFA9EC`), `renderSyncedScrubber(steps, devices)` (20px track, bg secondary, radius 4px; one absolute segment per step with `left`/`width` % computed from `startMs`/`durationMs`/`totalMs`, color per device or linear gradient for parallel; playhead 1px vertical line updated via `timeupdate`). Also extend `toTestViewModel()` (line ~2389) — when `multiDevice` present, project `perDeviceArtifacts` into per-device video/log URL fields; otherwise existing shape UNTOUCHED.
- [x] T021 Add synced-playback JS to the inline `<script>` block in `packages/report-web/src/renderers.ts` (lines ~1350-1800). All selectors scoped by `data-test-id` container. Functions: `selectStep(testId, stepIndex, perDeviceOffsets)` — seek each device's `<video>` to `perDeviceOffsets[dev]/1000` seconds; toggle `.selected` on the matching `.step-bubble`. `togglePlayPause(testId)` — toggle play/pause across both videos; swallow autoplay rejection with `.catch(() => {})`. `onTimelineClick(testId, event)` — compute ratio from click X, convert to seconds via `totalMs`, set `currentTime` on both videos. Wire `timeupdate` on the first video to move the playhead.
- [x] T022 Audit fail-fast behaviors end-to-end in the orchestrator: (a) action dispatch failure on either device → abort test as FAIL within 5s; surviving recording stopped cleanly; (b) gRPC disconnect → abort as FAIL; (c) planner returns duplicate-device actions → retry `planMulti()` once, then abort as FAIL; (d) planner returns an action whose `device` key is not present in the validated `devices.yaml` manifest → retry `planMulti()` once, then abort as FAIL with reason `planner-malformed: unknown device "{key}"` (spec §Parallel vs sequential action dispatch scenario); (e) either planner response includes `PLANNER_ACTION_FAILED` → record test FAIL; (f) watchdog threshold exceeded → abort as FAIL. Verify no single-device abort code path was touched. **AUDIT RESULT**: (a) MultiDeviceOrchestrator step 8 (dispatch fail-fast) returns failure with reason. (b) step 2 capture / step 7 dispatch catches errors and returns failure. (c)(d) planMulti `parseMultiDevicePlannerResponse` throws on unknown-device / duplicate-device; retry loop uses `MAX_LLM_ATTEMPTS=2` (one retry) then throws; orchestrator step 3 wraps planner in try/catch producing failure. (e) step 6 terminal check on `PLANNER_ACTION_FAILED`/`PLANNER_ACTION_COMPLETED`. (f) step before 1, watchdog `_stuckIterations > 5` emits pinned reason. Confirmed single-device `ActionExecutor.ts` and `TestExecutor.ts` zero-diff vs main. <!-- clarified: 2026-04-15 — added explicit unknown-device-key retry-then-fail audit case and watchdog abort per spec. -->
- [x] T023 Final single-device preservation audit: `git diff main` touching any of `TestExecutor.ts`, `ActionExecutor.ts`, `planner.md`, `sessionRunner.ts`, `testLoader.ts`, `testCompiler.ts` must show zero behavioral changes. Run both Android and iOS single-device smoke tests (existing `auth/login.yaml`) and diff the resulting `run.json` + artifact tree against a pre-change baseline — require byte-identical output. **AUDIT RESULT**: `git diff main --stat` on all six preservation-critical files produces empty output (zero bytes changed). Touched files vs main (`packages/cli/src/testRunner.ts`, `testSelection.ts`, `workspace.ts`, `packages/common/src/models/*.ts`, `packages/device-node/src/device/RecordingManager.ts`, `Device.ts`, `packages/goal-executor/src/ai/AIAgent.ts`, `index.ts`) are all additive or backward-compatible: new early-exit branches, new sibling methods, new optional fields with absent-field-omit JSON semantics. Live emulator smoke (byte-identical run.json diff) deferred to T028 (manual E2E) since no live emulator fleet is available in this worktree.

## Phase 4: Polish

<!-- Fixtures, regression artifacts, quickstart doc. Only items required for ship. -->

- [x] T024 [P] Add fixture `.finalrun/multi-device/` example under `packages/cli/tests/fixtures/` demonstrating a 2-device `devices.yaml` plus a `tests/chat/send_message.yaml` that mirrors the sandwich mock's step list. Do not ship an actual workspace — fixture is test-only. **IMPLEMENTED**: Inlined as `writeFixtureTree()` tmpdir-based helper in `multiDeviceTestLoader.test.ts` and `multiDeviceTestCompiler.test.ts` — avoids checked-in fixture tree while giving each test suite full isolation. `tests/chat/send_message.yaml` fixture mirrors sandwich-mock step list (alice composes, bob observes).
- [x] T025 [P] Unit tests: loader validation matrix in `packages/cli/tests/multiDeviceTestLoader.test.ts` — cover each rejection rule (≠2 devices, cross-platform, non-Android v1, duplicate keys, step missing `${devices.X}`, unknown device key reference). Compiler test — `${variables.*}` substituted; `${devices.*}` and `${secrets.*}` preserved literally. **IMPLEMENTED**: `packages/cli/src/multiDeviceTestLoader.test.ts` (10 tests, all pass: ok 92-101) covers full validation matrix. `packages/cli/src/multiDeviceTestCompiler.test.ts` (4 tests, all pass: ok 88-91) covers interpolation, token preservation, `Devices:` header emission, unknown-variable round-trip.
- [x] T026 [P] Snapshot test for `AIAgent.planMulti()` in `packages/goal-executor/tests/multiDevicePlanner.test.ts` — mock 2-device state + minimal history → assert well-formed `MultiDevicePlannerResponse`. Separate case: mock returns duplicate-device actions → assert retry occurs and terminal error thrown after second failure. Additional case: mock returns an action with an unknown device key (not in `devices.yaml`) → assert retry occurs and terminal error thrown after second failure with message naming the unknown key. Confirm existing `plan()` snapshot test passes unchanged (regression guard). <!-- clarified: 2026-04-15 — added unknown-device-key retry case to mirror orchestrator fail-fast audit (T022 case d). --> **IMPLEMENTED**: `packages/goal-executor/src/ai/multiDevicePlanner.test.ts` (4 tests, all pass: ok 50-53). Covers: well-formed 1-device response round-trip; duplicate-device retry-then-throw; unknown-device retry-then-throw naming key; >2-actions retry-then-throw. Existing `plan()` snapshot suite passes unchanged.
- [x] T027 Integration test: parallel `startRecording` on one logical `(runId, testId)` with distinct `deviceId` values validates the RecordingManager fix (T004) — both recordings start successfully, key collision is avoided, and `stopRecording` called with the same `(runId, testId, deviceId)` trio tears each down independently. **IMPLEMENTED**: 5 new tests in `packages/device-node/src/device/RecordingManager.test.ts` (all pass: ok 19-23). Covers: 2-arg byte-identical key; 3-arg scoped key; 2-arg vs 3-arg differ; deviceId sanitization; parallel Alice+Bob start on same (runId, testId) without key collision.
- [ ] T028 E2E smoke (manual for v1): two Android emulators auto-boot; run `finalrun test multi-device/tests/chat/send_message.yaml`; verify COMPLETED status and that the rendered report matches the sandwich mock (colors, three-column layout, chat bubble alignment, synced scrubber, click-to-seek both videos). **DEFERRED**: requires live 2-emulator fleet not available in this worktree. To be executed pre-ship on developer machine with connected Android emulators.
- [x] T029 Add a short quickstart section to `docs/memory/cli/multi-device-workspace.md` (new file under the existing memory tree) describing `.finalrun/multi-device/` layout, the `devices.yaml` schema, token syntax, and CLI entry points (`finalrun test multi-device/tests/...`). One page maximum. Cross-reference from `docs/memory/index.md`. **IMPLEMENTED**: `docs/memory/cli/multi-device-workspace.md` covers workspace layout, devices.yaml schema, token syntax, CLI entry points, report layout, fail-fast behavior. Cross-referenced from `docs/memory/cli/index.md`.

---

## Execution Order

- Phase 1 is fully parallelizable for T001–T004 (`[P]`); T005–T009 depend on types from T001/T002 being present.
- Phase 2: T010 and T011 can run in parallel; T012 requires T011; T012a (gRPC concurrency spike) gates T013/T014 and MUST complete before any orchestrator code is written; T013 requires T001 and T012a outcome; T014 requires T011, T012, T013 (and existing `ActionExecutor` class); T015/T016 extend T014.
- Phase 3: T017 requires T014; T018 requires T002/T003 (types) and is independent of orchestrator code; T019/T020/T021 depend on T002/T003 types but are independent of orchestrator (can be built against fixture data); T022/T023 gate the ship.
- Phase 4: T024–T029 parallelize across reviewers; T028 requires all of Phase 3 complete.
